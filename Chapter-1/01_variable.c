#include <stdio.h>

int main() {
  int Akrit;
  int Mann;
  int Coding;
  // int akrit mann singh; it is not allowed means -space in character not
  // allowed in variable name.
  int a; // variable declaration
  a = 57317;
  Akrit = 02;
  Mann = 11;
  Coding = 2011;
  printf("The output of the program is %d", Akrit );
  printf("%d", Mann );
  printf( "%d", Coding);
  return 0;
}
